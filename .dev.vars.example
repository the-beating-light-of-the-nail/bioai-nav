# 复制为 .dev.vars（已 gitignore）供 wrangler dev 使用；线上等值经 wrangler secret put 写入
UPSTASH_REDIS_REST_URL=https://concise-snail-221788.upstash.io
UPSTASH_REDIS_REST_TOKEN=<见 ~/.agents/skills/upstash-redis/connection.env>
BIOAINAV_ADMIN_TOKEN=<随机 hex，生成命令：node -e "console.log(require('crypto').randomBytes(24).toString('hex'))">
