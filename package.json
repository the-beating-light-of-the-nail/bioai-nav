{
  "name": "bioai-nav",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "check": "node scripts/check-prerender.mjs",
    "deploy": "npm run generate && node scripts/check-prerender.mjs && wrangler deploy"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.2.5",
    "nuxt": "3.21.11",
    "vue": "^3.5.41"
  }
}
